public enum StatType
{
    HP,
    Speed,
    AttackSpeed,
    Damage,
    Defence
}

public enum SkillType
{
    FireBall,
    IceBolt,
    Lightning,
    SquidInk
}

public enum ModificationType
{
    Plus,
    Multiply
}

public enum ProcessType
{
    Single,
    Loop
}
